# Plan 008 (pivote)

1. Token de gestión (hash SHA-256) en needs/pets/places.  
2. Módulo `manage` (preview + close).  
3. UI `/cerrar` + copy en formularios de publicación.  
4. Remover UI `/moderacion`; API moderation opcional ops.  
5. Docs Fase 9 → siguiente 10.
