# Plan 009 — Producción

1. data-source + migration baseline.  
2. Health live/ready + connectors status.  
3. OPS_TOKEN guard en connectors (prod).  
4. Docker backend + compose.prod.  
5. Export OpenAPI + docs deploy + roadmap.
