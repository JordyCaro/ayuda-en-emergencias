# Connector IDEAM (runtime en backend)

La implementación vive en `apps/backend/src/connectors/ideam.connector.ts`.

Esta carpeta queda como ancla del monorepo (`connectors/co/ideam`) según el mapa del repo.
Cuando extragamos el paquete `@aee/connector-ideam`, el código se moverá aquí.

Endpoint MapServer documentado en `docs/sources/source-registry.md`.
