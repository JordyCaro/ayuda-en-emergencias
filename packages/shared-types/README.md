# packages/shared-types

Enums y DTO compartidos entre `apps/backend`, `apps/frontend` y `connectors/` (Verification, NeedCategory, EventType, etc.).

Sin código aún — ver `specs/001-mvp-mapa-ayuda/data-model.md`.
